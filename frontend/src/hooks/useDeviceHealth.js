// Hook para calcular/leer salud de un dispositivo (placeholder).
export default function useDeviceHealth(deviceId) {
  // Devolver 'verde' | 'amarillo' | 'rojo' según alertas activas
  return { health: 'verde' }
}
