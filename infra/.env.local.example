# Variables locales para desarrollo fuera de Docker
# Copia como .env.local en esta misma carpeta y ajusta según tu entorno.
# Este archivo NO se versiona (.gitignore contiene /infra/.env.local)

# --- Base de datos ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mkmonitor
DB_USER=mkadmin
DB_PASS=mkpassword

# URL completa (tiene prioridad sobre los campos anteriores)
DATABASE_URL=postgresql+psycopg2://mkadmin:mkpassword@localhost:5432/mkmonitor

# --- Backend ---
APP_ENV=dev

# --- Rate limiting ---
# Para evitar dependencia de Redis en local
REDIS_URL=memory://

# --- JWT y claves (ejemplos de desarrollo) ---
JWT_SECRET=changeme_jwt_secret
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=60

# --- DeepSeek (opcional) ---
# DEEPSEEK_API_KEY=
# DEEPSEEK_API_URL=https://api.deepseek.com/v1/chat/completions
# DEEPSEEK_MODEL=deepseek-chat
# AI_ANALYSIS_PROVIDER=auto
# AI_TIMEOUT_SEC=20
# AI_MAX_TOKENS=800
