services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: mkmonitor
      POSTGRES_PASSWORD: mkmonitor
      POSTGRES_DB: mkmonitor
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mkmonitor"]
      interval: 5s
      timeout: 5s
      retries: 5
